// Package pkg holds gh-plt's implementation: the template catalog, the
// language/framework/package-manager ecosystem catalog, project detection,
// persistent configuration, the interactive prompts, workflow rendering,
// GitHub API version resolution, and dependency installation.
package pkg
